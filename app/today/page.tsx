import Link from 'next/link';import {Clock3,Trophy} from 'lucide-react';import {ContentPage,PageIntro} from '@/components/SitePrimitives';export default function Today(){const date=new Intl.DateTimeFormat('en-US',{dateStyle:'long',timeZone:'UTC'}).format(new Date());return <ContentPage><PageIntro eyebrow="Live until midnight UTC" title="Today’s ranking"><p>Today has its own live board. Only verified boosts made during the current UTC day count here.</p></PageIntro><section className="rounded-[28px] border-2 border-[#fb923c] bg-[#fff7ed] p-5 sm:p-7"><div className="flex flex-wrap items-center justify-between gap-3"><div><div className="flex items-center gap-3"><h2 className="text-xl font-black">{date}</h2><span className="rounded-full bg-[#fb923c] px-3 py-1 text-xs font-black text-white">● Live</span></div><p className="mt-2 text-sm text-[#fb923c]">This board resets automatically at midnight UTC.</p></div><span className="flex items-center gap-2 text-sm font-bold text-[#fb923c]"><Clock3 size={17}/> Open now</span></div><div className="mt-6 rounded-[22px] bg-white px-6 py-12 text-center"><Trophy className="mx-auto text-[#fb923c]" size={34}/><h3 className="mt-4 text-2xl font-black">Claim today’s #1 spot</h3><p className="mx-auto mt-2 max-w-md text-[#817a75]">No verified boosts have been placed today. Start the board with a $1 boost.</p><Link href="/submit" className="btn btn-primary mt-6">Add product & boost</Link></div></section></ContentPage>}
